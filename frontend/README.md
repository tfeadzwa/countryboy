# Countryboy Frontend

This is the frontend for the Countryboy bus ticketing application (Vite + React + TypeScript).

Quickstart

```sh
cd frontend
npm install
npm run dev
```

Tech stack

- Vite
- React
- TypeScript
- Tailwind CSS

Deployment

Build the production assets with `npm run build` and serve them using your preferred static host.
